<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Earshot{% endblock %}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='style.css') }}">
</head>
<body>
    <!-- ===== FLASH MESSAGES (shown on every page) ===== -->
    {% with messages = get_flashed_messages() %}
      {% if messages %}
        <div class="flash-messages">
          {% for msg in messages %}
            <div class="flash">{{ msg }}</div>
          {% endfor %}
        </div>
      {% endif %}
    {% endwith %}
    <!-- ================================================= -->

    <div class="container">
        <header>
            <h1><a href="{{ url_for('index') }}" style="color:inherit; text-decoration:none;">Earshot</a></h1>
            {% if session.user_id %}
            <nav>
                <a href="{{ url_for('post') }}">Post</a> |
                <a href="{{ url_for('logout') }}">Logout</a>
            </nav>
            {% endif %}
        </header>

        {% block content %}{% endblock %}
    </div>
</body>
</html>
